// This file contains other structs, tilemaps, etc.. that I don't want to clutter main with 

typedef struct Map { // Map struct for creating the 2d tile maps
	int mapX, mapY;
	int tileMap;
} Map;

// Map data list
Map prologue = {10, 10, 1};
